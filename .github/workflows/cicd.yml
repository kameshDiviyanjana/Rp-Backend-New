# name: CI/CD for Node.js & Flask Microservices

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build-test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       # Setup Node.js & Install Dependencies
#       - name: Set Up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 18

#       - name: Install Node.js Dependencies
#         run: |
#           cd node-service
#           npm install

#       - name: Run Node.js Tests
#         run: |
#           cd node-service
#           npm test

#       # Setup Python & Install Dependencies
#       - name: Set Up Python
#         uses: actions/setup-python@v3
#         with:
#           python-version: "3.10"

#       - name: Install Flask Dependencies
#         run: |
#           cd flask-service
#           pip install -r requirements.txt

#       - name: Run Flask Tests
#         run: |
#           cd flask-service
#           pytest

#   docker-build-push:
#     needs: build-test
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Log in to Docker Hub
#         run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

#       - name: Build and Push Node.js Service
#         run: |
#           docker build -t mydockerhubuser/node-service:latest ./node-service
#           docker tag mydockerhubuser/node-service:latest mydockerhubuser/node-service:${{ github.run_number }}
#           docker push mydockerhubuser/node-service:latest
#           docker push mydockerhubuser/node-service:${{ github.run_number }}

#       - name: Build and Push Flask Service
#         run: |
#           docker build -t mydockerhubuser/flask-service:latest ./flask-service
#           docker tag mydockerhubuser/flask-service:latest mydockerhubuser/flask-service:${{ github.run_number }}
#           docker push mydockerhubuser/flask-service:latest
#           docker push mydockerhubuser/flask-service:${{ github.run_number }}

#   deploy:
#     needs: docker-build-push
#     runs-on: ubuntu-latest
#     steps:
#       - name: Deploy to Server
#         env:
#           SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
#           SERVER_USER: ${{ secrets.SERVER_USER }}
#           SERVER_HOST: ${{ secrets.SERVER_HOST }}
#         run: |
#           echo "$SSH_PRIVATE_KEY" > private_key && chmod 600 private_key
#           ssh -o StrictHostKeyChecking=no -i private_key $SERVER_USER@$SERVER_HOST << 'EOF'
#             cd /path/to/microservices
#             docker-compose down
#             docker pull mydockerhubuser/node-service:latest
#             docker pull mydockerhubuser/flask-service:latest
#             docker-compose up -d
#           EOF

# name: Deploy Flask API to EC2

# on:
#   push:
#     branches: [fix/new-intigartion-all]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up SSH key
#         run: |
#           echo "${{ secrets.EC2_SSH_KEY }}" | base64 -d > key.pem
#           chmod 600 key.pem

#       - name: Deploy and run Flask API
#         run: |
#           ssh -o StrictHostKeyChecking=no -i key.pem ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
#             cd ${{ secrets.PROJECT_PATH }}
#             git pull origin main
#             source venv/bin/activate
#             pip install -r requirements.txt

#             # Kill existing process if running
#             pkill -f api.py || true

#             # Run in background using nohup
#             nohup python api.py > output.log 2>&1 &
#           EOF

name: Deploy Python Flask API to EC2

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" | tr -d '\r' | sed 's/\\n/\n/g' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

      - name: Upload Flask App to EC2
        run: |
          rsync -avz -e "ssh -i ~/.ssh/deploy_key" ./ ubuntu@${{ secrets.EC2_HOST }}:/home/ubuntu/flask-api

      - name: Install Python Dependencies & Run Flask App
        run: |
          ssh -i ~/.ssh/deploy_key ubuntu@${{ secrets.EC2_HOST }} << 'EOF'
            cd /home/ubuntu/flask-api

            # Install Python and pip if not installed
            sudo apt update
            sudo apt install -y python3 python3-pip python3-venv

            # Set up virtual environment
            python3 -m venv venv
            source venv/bin/activate

            # Install dependencies
            pip install -r requirements.txt

            # Kill existing process running api.py (optional)
            pkill -f api.py || true

            # Run Flask app in background
            nohup python api.py > flask.log 2>&1 &
          EOF
